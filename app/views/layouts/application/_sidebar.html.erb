<nav id="sidebarMenu" class="col-md-3 col-lg-2 d-md-block sidebar collapse" style="background: #f5f5f5">
  <div class="position-sticky pt-3">
    <div class="text-center d-none d-md-block"><img src="<%= BROWN_BUCKET_HOST %>/logo-faded.png" alt="" width="122"></div>
    <h6 class="sidebar-heading d-flex justify-content-between align-items-center px-3 mt-4 mb-1 text-muted">
      <span>&nbsp;</span>
    </h6>
    <ul class="nav flex-column mb-2">
      <li class="nav-item">

        <%= button_to my_settings_path, class: 'btn nav-link brown-no-hover', method: :get  do %>
          <i class="bi bi-gear me-2"></i>
          My Settings
        <% end %>

        <% if current_user %>
          <%= button_to destroy_user_session_path, class: 'btn nav-link brown-no-hover', method: :delete  do %>
            <i class="bi bi-box-arrow-right me-2"></i>
            Sign Out
          <% end %>
        <% else %>
          <%= button_to new_user_session_path, class: 'btn nav-link brown-no-hover', method: :get  do %>
            <i class="bi bi-box-arrow-in-right me-2"></i>
            Sign In
          <% end %>
        <% end %>

      </li>
    </ul>
  </div>
</nav>
